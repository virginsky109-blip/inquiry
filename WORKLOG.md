# 작업 로그 (2026-07-20)

## 1. 내륙운송료(TRUCK) 자동 미갱신 버그 수정
- 원인: 요율 매칭 실패 시 쓰는 `auto` 플래그와 "사용자가 직접 값을 고쳤다"는 의미가 뒤섞여 있었음. 한 번이라도 매칭에 실패하면 이후 요율을 갱신해도 예전 값(대개 0)에 영구 고정되던 문제.
- 조치: `manual` 플래그를 새로 분리해 "사용자가 직접 입력한 값"만 보존하고, 자동 매칭은 매번 다시 시도하도록 수정 (`index.html`의 `mk()`, `refreshTruck()`).
- 부수 수정: 여러 요율표가 동점으로 매칭될 때 오래된 표가 우선하던 것을, 나중에 추가/수정한 표가 우선하도록 변경(`findRate` 동점 처리).

## 2. 안전운임(내륙운송료) 국토부 고시 전국표 지원
- 국토부 "수출입 컨테이너 품목 기점별 운임(편도/왕복)" xlsx(항만별 시트, 전국 읍·면·동, 40FT/20FT × 안전위탁운임/운수사업자간운임/안전운송운임 3종) 전용 파서 추가.
- **안전운송운임만** 추출하고 안전위탁운임·운수사업자간운임은 버림.
- 행이 많아(최대 약 9만 7천 행) localStorage 용량을 넘길 수 있어, 3,000행 초과 세트는 자동으로 IndexedDB에 저장하도록 처리.
- 항구명 매칭을 정확 일치 우선 + 부분 일치(예: "부산" → "부산신항"/"부산북항") 완화 매칭으로 개선.

## 3. 안전운임에도 3단계 우선순위 적용
- 기존에는 부대비용(rate_master)에만 있던 "① 고객사 특약 → ② 개인 표준 → ③ 전사 공통" 우선순위를, 안전운임(safe_rate)에도 동일하게 적용.
- 요율 관리 목록에서 안전운임 세트도 적용 범위(공통/개인 표준/특약) 선택이 가능해짐.

## 4. 전사 공통 데이터를 HTML 내장 → Supabase DB 관리로 전환
- 기존에 `index.html`에 하드코딩돼 있던 `SEED_COMMON_RATES`(THC/SEAL/WFG 등)를 완전히 제거.
- Supabase에 안전운임 전용 테이블 `safe_rates_common`(port/place/size/amount 컬럼 + 인덱스) 추가 — 수만 행을 JSONB 한 덩어리 대신 실제 컬럼으로 저장해 인덱스 조회.
- 로그인 없이도(anon) `scope='common'` 데이터는 읽을 수 있도록 RLS 정책 추가(`rate_sets`, `safe_rates_common` 모두).
- 앱은 로그인 여부와 무관하게 부팅 시 Supabase에서 공통 부대비용·공통 안전운임을 조회하도록 변경.
- 안전운임 고시 전국표 업로드 시, 로그인 중이면 로컬이 아니라 Supabase로 직접 올라가도록 변경. 이때 **파일에 들어있는 기점(항만)만 교체**해서, 왕복표와 편도표를 순서대로 올려도 서로 지우지 않게 처리.

## 5. 수출/수입 전환 시 출발·도착 구간 자동 스왑
- 수출은 출발항(POL)이, 수입은 도착항(POD)이 한국이어야 하므로, 방향(수출/수입) 전환 시 출발·도착 국가/항구 값을 서로 맞바꾸도록 수정(`swapRoute()`).
- 전환 전에 입력해둔 값도 사라지지 않고 반대쪽 칸으로 이동.

## 6. 인코텀즈 방향별 대칭 로직 수정
- 운임(해상/항공): 수출은 C/D조건(CFR·CIF·CPT·CIP·DAP·DDP), 수입은 반대인 E/F조건(EXW·FCA·FOB)에서 뜨도록 방향별로 분리. 기존엔 수출 기준 조건을 수입에도 그대로 써서 방향이 거꾸로였음.
- 도착지 비용(THC/통관/배송): 수출은 D조건에서만, 수입은 D조건을 제외한 나머지 전부에서 뜨도록 분리. 수입일 땐 항목 라벨도 "해외발생비용" → "국내발생비용"으로 자동 표시.
- 국내(한국측) 원가 항목(TRUCK·수출통관·THC·SEAL·WFG·항만안전관리비·항만보안료·CFS·항공 관련 비용)에 DAP·DDP 조건이 통째로 빠져있던 것을 추가 — 수출 DDP에서도 국내 비용이 정상적으로 표시됨.
- 검증: 수출 DDP·수입 EXW(양쪽 모두 부담)와 수출 CIF·수입 CIF(기존 정상 케이스) 네 가지 조합 모두 재검증 완료.

## 남은 수동 작업 (담당자가 직접 해야 함)
1. **Supabase 대시보드 → SQL Editor**에서 갱신된 `supabase_schema.sql` 전체를 재실행 (테이블 `safe_rates_common` 생성 + RLS 정책 추가, 1회). — 이 저장소의 publishable key만으로는 실행 불가하며, 담당자의 Supabase 로그인이 필요해 코드로 대신할 수 없음.
2. 앱에서 **클라우드 로그인** 후, 안전운임 고시 전국표(편도/왕복)를 다시 한 번씩 업로드 — 이번에는 로컬이 아니라 팀 공용 DB로 올라감.
